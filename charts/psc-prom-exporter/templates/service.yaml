apiVersion: v1
kind: Service
metadata:
  name: {{ include "psc-prom-exporter.fullname" . }}
  labels:
    {{- include "psc-prom-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "psc-prom-exporter.selectorLabels" . | nindent 4 }}
